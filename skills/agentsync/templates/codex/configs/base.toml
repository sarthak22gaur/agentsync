# Codex configuration for {{PROJECT_NAME}}
# Fragments under codex/configs/ are concatenated into .codex/config.toml on sync.

# Default Codex model — the latest GPT. Applies to every Codex agent (the agent
# TOMLs set only reasoning effort). Change it if your Codex setup uses a
# different model, or a Claude model via an Anthropic-compatible endpoint.
model = "gpt-5.5"
